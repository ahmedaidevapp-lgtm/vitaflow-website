import PrivacyFrDoc from "./legal/PrivacyFrDoc";

/** Stable `/privacy/fr` URL — always the French text, regardless of the UI language. */
const PrivacyFr = () => <PrivacyFrDoc />;

export default PrivacyFr;
